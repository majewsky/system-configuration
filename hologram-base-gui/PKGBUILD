pkgname='hologram-base-gui'
pkgver='1.0.3'
pkgrel=1
pkgdesc='hologram: base GUI installation'
arch=('any')
url=''
license=('AGPL3')
depends=() # runtime dependencies are listed below
source=()

package() {
    depends=(
        'hologram-base'
        'hologram-multimedia-base'
        'xorg-server'
        # assorted GUI apps
        $(pacman -Sqg xorg-apps)
        'cutegram'
        'firefox'
        'graphviz'
        'gnaural'
        'gnaural-presets'
        'gnuplot'
        'inkscape'
        'mumble'
        'mupdf'
        'qgit'
        'paprefs'
        'pavucontrol'
        'pidgin-otr' # includes pidgin
        'screen-message'
        'sxiv'
        'thunderbird'
        'tk'         # for gitk and git-gui
        'vimprobable2'
        'vlc'
        'xsel'
        # helper packages (icons, codecs, etc.)
        'aspell-de'  # to silence complaints in gitk about missing spell check dict
        'gstreamer0.10-ffmpeg'
        'human-icon-theme'
        # font packages
        'adobe-source-sans-pro-fonts'
        'cantarell-fonts'
        'ttf-bitstream-vera'
        'ttf-dejavu'
        'ttf-droid'
        'ttf-freefont'
        'ttf-liberation'
        'ttf-linux-libertine'
        'ttf-oxygen'
        'ttf-ubuntu-font-family'
    )
}
