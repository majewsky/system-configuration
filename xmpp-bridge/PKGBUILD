pkgname=xmpp-bridge
pkgver=0.5.0
pkgrel=1
pkgdesc="Connect command-line programs to XMPP"
arch=('i686' 'x86_64')
url='https://github.com/majewsky/xmpp-bridge'
license=('GPL3')
depends=('libstrophe')
source=("https://github.com/majewsky/${pkgname}/archive/v${pkgver}.tar.gz")
sha256sums=('5911bf70f7c9cc716b926fd788e188e81e91aae0b224e6bd584aaf7a154300bb')

build() {
    cd "${srcdir}/${pkgname}-${pkgver}"
    make
}

package() {
    cd "${srcdir}/${pkgname}-${pkgver}"
    make install DESTDIR="${pkgdir}"
}
