pkgname=xmpp-bridge
pkgver=0.2.0
pkgrel=1
pkgdesc="Connect command-line programs to XMPP"
arch=('i686' 'x86_64')
url='https://github.com/majewsky/xmpp-bridge'
license=('GPL3')
depends=('libstrophe')
source=("https://github.com/majewsky/${pkgname}/archive/v${pkgver}.tar.gz")
sha256sums=('7be1b97dba3304ca5e0957b07176d0de339ef65399dd850a8916e7ea536344aa')

build() {
    cd "${srcdir}/${pkgname}-${pkgver}"
    make
}

package() {
    cd "${srcdir}/${pkgname}-${pkgver}"
    make install DESTDIR="${pkgdir}"
}
